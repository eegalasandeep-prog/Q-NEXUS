version: '3.8'

services:
  qnexus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qnexus-production
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
      - PYTHONUNBUFFERED=1
    volumes:
      - qnexus-data:/app/data
    restart: unless-stopped

volumes:
  qnexus-data:
